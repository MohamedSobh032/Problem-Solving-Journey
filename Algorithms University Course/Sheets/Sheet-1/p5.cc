/*
5. [Brute-force] The graph coloring problem is an assignment of a color 
to 
each node of a graph with the constraint that no two 
connected/adjacent nodes have the same color while using the 
minimum number of colors. Write a program to find the minimum 
number of colors needed for a given graph. 
*/

int colors;


void func(vector<vector<int>> v) {
    
    for (int i = 0; i < v.size(); i++) {
        for (int j = 0; j < v)
    }
}

int main() {

    vector<vector<int>> v(n, vector<int>(m));
    func(v);
    cout << colors;

}
